name: playground
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  async:
  flutter:
    sdk: flutter

  flutter_hooks:
  freezed_annotation:
  go_router: ^17.2.0
  hooks_riverpod: ^3.3.1
  riverpod_annotation:

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  go_router_builder:
  mocktail:
  riverpod_generator:
  very_good_analysis:

flutter:
  uses-material-design: true
